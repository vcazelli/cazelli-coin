type Query {
  helloWorld: String!
  getWallet(id: ID!): Wallet!
  listWallet: [Wallet]
  getQuotation(id: ID!): Quotation!
  listQuotation: [Quotation]
}

type Wallet {
  id: ID!
  name: String!
  balance: Float!
}

type Quotation {
  id: ID!
  coin: String!
  price: Float!
  date: String!
}