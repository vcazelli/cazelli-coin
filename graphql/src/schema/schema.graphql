type Query {
  helloWorld: String!
  getWallet(id: ID!): Wallet
  listWallet: [Wallet]
  getQuotation(): Quotation!
  listQuotation: [Quotation]
}

type Wallet {
  id: ID!
  name: String!
  balance: Float!
}

type Quotation {
  coin: String!
  price: Float!
  date: String!
}